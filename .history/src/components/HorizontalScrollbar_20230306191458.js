import React from 'react'
import { Box }

const HorizontalScrollbar = ({ data }) => {
  return (
    <div>
      {data.map((item) => 
      <Box>

      </Box>
      )}
      </div>
      )}

export default HorizontalScrollbar
